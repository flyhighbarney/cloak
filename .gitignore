# This repository holds one project, cloakline/. Other projects live alongside it
# in the same directory but are separate repositories with their own history and
# remotes — listed here so they do not show up as untracked noise.
#
#   wiretype/  -> github.com/flyhighbarney/wiretype
#   redflag/   -> its own checkout
wiretype/
redflag/

# Local agent configuration, not part of the project.
.claude/
