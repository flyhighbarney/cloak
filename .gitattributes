# Force LF line endings for anything a POSIX shell must execute or read.
# Windows checkouts of these files must still have LF; otherwise bash on
# macOS/Linux errors with "/usr/bin/env: 'bash\r': No such file or directory".
*.sh    text eol=lf
*.plist text eol=lf
Makefile text eol=lf
